# AES-256-GCM key for InsuranceCheckLead PHI encryption. 32 bytes base64-encoded.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
INSURANCE_LEAD_ENCRYPTION_KEY=
